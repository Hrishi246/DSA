# def subdomainVisits(cpdomains):
#     domain_dict = dict()
#     for cpdomain in cpdomains:
#         long_domain_count, long_domain = cpdomain.split(" ")
#         domain_dict[long_domain] = long_domain_count
#         short_domains = long_domain.split(".")
#         while len(short_domains) > 0:
#             if short_domains[1] not in domain_dict.keys():
#                 domain_dict[short_domains[1]] = 0
#             else:
#                 domain_dict[short_domains[1]] = + 1
#
# subdomainVisits(["9001 discuss.leetcode.com"])

print("this")
print("that")